name: Get Latest Tag Example

on:
  push:
    branches:
      - main
    tags:
      - '*'

jobs:
  get-latest-tag:
    runs-on: ubuntu-latest

    steps:
    - name: Show latest tag
      run: echo "Latest tag is ${{ github.ref_name }}"
